import { MAIN_MENU_SCENE } from "../constants/scenes";

/**
 * Сцена главного меню
 */
export default class MainMenu extends Phaser.Scene {
    constructor() {
        super(MAIN_MENU_SCENE);
    }

    /**
     * 
     */
    public create() {
        
    }
}
